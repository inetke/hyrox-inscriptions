name: Keep Streamlit Awake

on:
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Streamlit app
        run: curl -L --fail --silent --show-error https://rfhyroxclub.streamlit.app